# Copy this file to `.env` and fill in the values.

# Mapbox public access token — used by <MapView /> in src/components/ui/MapView.tsx
# Get one at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
